to start the application: 
1. bundle install 
2. rake db:create (i'm using MySQL, so you have to :( ) 
3. rake db:migrate 
4. rake db:test:prepare 
5. rake db:seed 
6. rails s 
7. guard (to run rspec) 